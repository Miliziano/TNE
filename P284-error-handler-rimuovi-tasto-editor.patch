diff --git a/src/nodes/types/error_handler/MappingPanel.tsx b/src/nodes/types/error_handler/MappingPanel.tsx
index 7d618b1..35565e1 100644
--- a/src/nodes/types/error_handler/MappingPanel.tsx
+++ b/src/nodes/types/error_handler/MappingPanel.tsx
@@ -47,7 +47,6 @@ export function ErrorHandlerNodesPanel({ nodeId }: { nodeId: string }) {
   const node           = useFlowStore((s) => s.nodes.find((n) => n.id === nodeId))
   const nodes          = useFlowStore((s) => s.nodes)
   const updateAdvanced = useFlowStore((s) => s.updateNodeAdvanced)
-  const openNodeEditor = useFlowStore((s) => s.openNodeEditor)
 
   if (!node) return null
   const laneId = node.data.laneId
@@ -163,12 +162,6 @@ export function ErrorHandlerNodesPanel({ nodeId }: { nodeId: string }) {
                       opacity: onErrorEmitsCatch(onError) ? 0.35 : 1 }}
                     onChange={(e) => updateAdvanced(n.id, 'critical' as any, e.target.checked ? 'true' : 'false')} />
                 </div>
-
-                {/* Apri editor nodo */}
-                <button onClick={() => openNodeEditor(n.id)} title="Open node editor"
-                  style={{ background: 'none', border: '1px solid #2a3349', borderRadius: 4, padding: '3px 5px', cursor: 'pointer', color: '#9a9aaa', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
-                  <i className="ti ti-external-link" style={{ fontSize: 11 }} />
-                </button>
               </div>
             )
           })}
