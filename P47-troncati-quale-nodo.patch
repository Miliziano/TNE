diff --git a/src-tauri/src/engine/executor.rs b/src-tauri/src/engine/executor.rs
index 184685a..9d52656 100644
--- a/src-tauri/src/engine/executor.rs
+++ b/src-tauri/src/engine/executor.rs
@@ -592,9 +592,12 @@ pub async fn execute_lane(
                     node_label: node_id_str.clone(),
                     level:      "warn".to_string(),
                     row_num:    0,
+                    // Il nodo va NEL TESTO: il pannello non antepone il
+                    // node_label alle righe NodeLog, e con più nodi troncati
+                    // si leggerebbero righe identiche senza sapere di chi.
                     message:    format!(
-                        "Pipeline a valle interrotta: le {} righe emesse non sono state elaborate del tutto",
-                        stats.rows_out,
+                        "{}: pipeline a valle interrotta, le {} righe emesse non sono state elaborate del tutto",
+                        node_id_str, stats.rows_out,
                     ),
                     target:     "panel".to_string(),
                 });
