diff --git a/src/components/Toolbar.tsx b/src/components/Toolbar.tsx
index 1e4206a..53496d2 100644
--- a/src/components/Toolbar.tsx
+++ b/src/components/Toolbar.tsx
@@ -1177,8 +1177,14 @@ export function Toolbar() {
   // piccolo manifesto. È il dato che il runner headless eseguirà.
   const esportaArtifact = async (monitorUrl: string, platform: string) => {
     if (!isTauri()) { addLog('error', 'Esportazione disponibile solo nell\'app desktop.'); return }
-    const { nodes, edges, pool, environments } = useFlowStore.getState()
+    const { nodes, edges, pool, environments, currentPath } = useFlowStore.getState()
     const runId = `export-${Date.now()}`
+    // Nome del piano = nome del file .ffplan aperto (senza percorso ne' estensione);
+    // null se il progetto non e' ancora stato salvato. Viaggia nel manifesto -> header
+    // del log -> evento RunStarted, cosi' il monitor mostra il NOME, non solo il codice.
+    const planName = currentPath
+      ? currentPath.split(/[\\/]/).pop()!.replace(/\.ffplan$/i, '')
+      : null
     let plan
     try {
       plan = buildRustPlan(nodes, edges, pool, runId)
@@ -1191,6 +1197,7 @@ export function Toolbar() {
       formatVersion: 1,
       kind:          'flowpilot-artifact',
       exportedAt:    new Date().toISOString(),
+      planName,
       profile:       environments.active || '(default)',
       platform,
       monitor:       monitorUrl || null,
