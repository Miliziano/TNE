diff --git a/src/components/Toolbar.tsx b/src/components/Toolbar.tsx
index b5b911a..4681483 100644
--- a/src/components/Toolbar.tsx
+++ b/src/components/Toolbar.tsx
@@ -469,7 +469,7 @@ function buildRustPlan(
   const { functions: userFns, errors: fnErrors } = parseUserFunctions(pool.userFunctions ?? [])
   if (fnErrors.length > 0) {
     const d = fnErrors.map((e) => `  • ${e.name ? e.name + ': ' : ''}${e.message}`).join('\n')
-    throw new Error(`Functions utente — ${fnErrors.length} errore/i:\n${d}`)
+    throw new Error(`User functions — ${fnErrors.length} error(s):\n${d}`)
   }
 
  // ── Bridge tra lane — accoppiati per channelName condiviso.
@@ -484,14 +484,14 @@ function buildRustPlan(
     for (const out of outNodes) {
       const name = (out.data.props?.['channelName'] as string) || ''
       if (!name) {
-        throw new Error(`Bridge Out "${out.data.label}": canale senza nome`)
+        throw new Error(`Bridge Out "${out.data.label}": channel without a name`)
       }
       const inNode = inNodes.find(
         n => ((n.data.props?.['channelName'] as string) || '') === name
       )
       if (!inNode) {
         throw new Error(
-          `Bridge "${name}": manca il Bridge In corrispondente (canale incompleto)`
+          `Bridge "${name}": the matching Bridge In is missing (incomplete channel)`
         )
       }
       bridges.push({
@@ -506,11 +506,11 @@ function buildRustPlan(
     for (const inn of inNodes) {
       const name = (inn.data.props?.['channelName'] as string) || ''
       if (!name) {
-        throw new Error(`Bridge In "${inn.data.label}": canale senza nome`)
+        throw new Error(`Bridge In "${inn.data.label}": channel without a name`)
       }
       if (!outNodes.some(n => ((n.data.props?.['channelName'] as string) || '') === name)) {
         throw new Error(
-          `Bridge "${name}": manca il Bridge Out corrispondente (canale incompleto)`
+          `Bridge "${name}": the matching Bridge Out is missing (incomplete channel)`
         )
       }
     }
@@ -776,12 +776,12 @@ function buildRustPlan(
           if (raw) {
             let fields: TransformFieldSpec[]
             try { fields = JSON.parse(raw as string) }
-            catch { throw new Error(`Transform "${node.data.label}": configurazione campi illeggibile`) }
+            catch { throw new Error(`Transform "${node.data.label}": unreadable fields configuration`) }
 
             const { compiled, errors } = compileTransformFields(fields, userFns)
             if (errors.length > 0) {
               const dettagli = errors.map(e => `  • ${e.message}`).join('\n')
-              throw new Error(`Transform "${node.data.label}" — ${errors.length} errore/i:\n${dettagli}`)
+              throw new Error(`Transform "${node.data.label}" — ${errors.length} error(s):\n${dettagli}`)
             }
 
             // Traduzione della scelta utente nel `mode` del motore.
@@ -839,7 +839,7 @@ function buildRustPlan(
             const raw = props['unionMapping']
             if (raw) fields = JSON.parse(raw as string)
           } catch {
-            throw new Error(`Union "${node.data.label}": mappatura campi illeggibile`)
+            throw new Error(`Union "${node.data.label}": unreadable field mapping`)
           }
 
           // Etichette leggibili degli handle, per il campo _union_source
@@ -881,7 +881,7 @@ function buildRustPlan(
 
           if (mode === 'pivot' && (!props['pivotField'] || !props['valueField'])) {
             throw new Error(
-              `Pivot "${node.data.label}": campo pivot e campo valore sono obbligatori.`)
+              `Pivot "${node.data.label}": pivot field and value field are required.`)
           }
           if (mode === 'unpivot') {
             let cols: unknown[] = []
@@ -902,8 +902,8 @@ function buildRustPlan(
           if (mode === 'pivot' && rawNull !== '' &&
               !/^-?\d+(\.\d+)?$/.test(rawNull) && rawNull !== 'true' && rawNull !== 'false') {
             console.warn(
-              `[pivot "${node.data.label}"] valore celle vuote "${rawNull}" è testuale: ` +
-              `se le colonne pivot sono numeriche, le operazioni a valle potrebbero fallire.`)
+              `[pivot "${node.data.label}"] empty cell value "${rawNull}" is text: ` +
+              `if the pivot columns are numeric, downstream operations may fail.`)
           }
           break
         }
@@ -939,7 +939,7 @@ function buildRustPlan(
             const s = props['dqConfig']
             if (s) raw = JSON.parse(s as string)
           } catch {
-            throw new Error(`Data Quality "${node.data.label}": configurazione illeggibile`)
+            throw new Error(`Data Quality "${node.data.label}": unreadable configuration`)
           }
 
           const rules = (raw.rules ?? []).map((r) => {
@@ -971,25 +971,25 @@ function buildRustPlan(
             const compile = (src: string | undefined, what: string) => {
               if (!src?.trim()) {
                 throw new Error(
-                  `Data Quality "${node.data.label}", regola "${r.label || r.id}": ` +
-                  `${what} richiede un'espressione.`)
+                  `Data Quality "${node.data.label}", rule "${r.label || r.id}": ` +
+                  `${what} requires an expression.`)
               }
               try { return parseExpression(src) }
               catch (e) {
                 const detail = e instanceof ExprParseError ? e.pretty() : String(e)
                 throw new Error(
-                  `Data Quality "${node.data.label}", regola "${r.label || r.id}":\n${detail}`)
+                  `Data Quality "${node.data.label}", rule "${r.label || r.id}":\n${detail}`)
               }
             }
 
-            if (r.checkType === 'custom') out.expression = compile(r.expression, "il check 'custom'")
-            if (r.repair === 'expression') out.repair_expression = compile(r.repairExpression, "il repair 'expression'")
+            if (r.checkType === 'custom') out.expression = compile(r.expression, "the 'custom' check")
+            if (r.repair === 'expression') out.repair_expression = compile(r.repairExpression, "the 'expression' repair")
 
             if (r.repair === 'lookup_from_file') {
               throw new Error(
-                `Data Quality "${node.data.label}", regola "${r.label || r.id}": ` +
-                `il lookup da file non è supportato. Carica il file con ` +
-                `source_file → materialize("tabella") e usa "lookup_from_materialize".`)
+                `Data Quality "${node.data.label}", rule "${r.label || r.id}": ` +
+                `file lookup is not supported. Load the file with ` +
+                `source_file → materialize("my_table") and use "lookup_from_materialize".`)
             }
 
             return out
@@ -1033,7 +1033,7 @@ function buildRustPlan(
             const raw = props['aggFunctions']
             if (raw) fns = JSON.parse(raw as string)
           } catch {
-            throw new Error(`Aggregate "${node.data.label}": configurazione funzioni illeggibile`)
+            throw new Error(`Aggregate "${node.data.label}": unreadable functions configuration`)
           }
 
           if (fns.length === 0) {
@@ -1047,9 +1047,9 @@ function buildRustPlan(
           const win = props['window'] ?? 'none'
           if (win !== 'none' && win !== '') {
             throw new Error(
-              `Aggregate "${node.data.label}": le finestre temporali non sono ` +
-              `supportate da questo nodo. Aggregate collassa le righe in gruppi ` +
-              `(GROUP BY) e materializza. Per le finestre serve un nodo dedicato.`)
+              `Aggregate "${node.data.label}": time windows are not ` +
+              `supported by this node. Aggregate collapses rows into groups ` +
+              `(GROUP BY) and materializes. For windows, use a dedicated node.`)
           }
 
           // `having` e i `filter` sono FPEL: lo studio li compila in IR
@@ -1103,7 +1103,7 @@ function buildRustPlan(
             const raw = props['windows']
             if (raw) defs = JSON.parse(raw as string)
           } catch {
-            throw new Error(`Window "${node.data.label}": configurazione funzioni illeggibile`)
+            throw new Error(`Window "${node.data.label}": unreadable functions configuration`)
           }
 
           if (defs.length === 0) {
diff --git a/src/i18n/it-en.json b/src/i18n/it-en.json
index 2a73529..3b47715 100644
--- a/src/i18n/it-en.json
+++ b/src/i18n/it-en.json
@@ -14,6 +14,7 @@
   "${fn.name} accetta al massimo ${fn.maxArgs} argomenti (ricevuti ${argCount}) — ${fn.usage}": "${fn.name} accepts at most ${fn.maxArgs} arguments (received ${argCount}) — ${fn.usage}",
   "${fn.name} richiede almeno ${fn.minArgs} argomenti (ricevuti ${argCount}) — ${fn.usage}": "${fn.name} requires at least ${fn.minArgs} arguments (received ${argCount}) — ${fn.usage}",
   "${functions.length} funzione/i valida/e: ": "${functions.length} valid function(s): ",
+  "${what} richiede un'espressione.": "${what} requires an expression.",
   "'Lane A si blocca finché Lane B non è pronta (reserved — implementazione futura).'": "'Lane A blocks until Lane B is ready (reserved — future implementation).'",
   "'true' se il nodo è marcato «escludi dal log»": "'true' if the node is marked «exclude from log»",
   "'true' se l'errore ha interrotto la lane": "'true' if the error stopped the lane",
@@ -153,6 +154,8 @@
   "Aggiungi un segreto (solo il nome; il valore arriva dal keychain a run-time)": "Add a secret (name only; the value comes from the keychain at run-time)",
   "Aggiungi una risorsa Kafka dalla resource strip, poi selezionala nel tab Connessione.": "Add a Kafka resource from the resource strip, then select it in the Connection tab.",
   "Aggiunto ${def.label}": "Added ${def.label}",
+  "Aggregate \"${node.data.label}\": configurazione funzioni illeggibile": "Aggregate \"${node.data.label}\": unreadable functions configuration",
+  "Aggregate \"${node.data.label}\": le finestre temporali non sono supportate da questo nodo. Aggregate collassa le righe in gruppi (GROUP BY) e materializza. Per le finestre serve un nodo dedicato.": "Aggregate \"${node.data.label}\": time windows are not supported by this node. Aggregate collapses rows into groups (GROUP BY) and materializes. For windows, use a dedicated node.",
   "Aggregazione": "Aggregation",
   "Albero vuoto": "Empty tree",
   "Algoritmo": "Algorithm",
@@ -242,6 +245,10 @@
   "Body XML grezzo": "Raw XML body",
   "Body testuale grezzo": "Raw text body",
   "Branch (opz.)": "Branch (opt.)",
+  "Bridge \"${name}\": manca il Bridge In corrispondente (canale incompleto)": "Bridge \"${name}\": the matching Bridge In is missing (incomplete channel)",
+  "Bridge \"${name}\": manca il Bridge Out corrispondente (canale incompleto)": "Bridge \"${name}\": the matching Bridge Out is missing (incomplete channel)",
+  "Bridge In \"${inn.data.label}\": canale senza nome": "Bridge In \"${inn.data.label}\": channel without a name",
+  "Bridge Out \"${out.data.label}\": canale senza nome": "Bridge Out \"${out.data.label}\": channel without a name",
   "BridgeIn elabora man mano che arrivano i dati.": "BridgeIn processes the data as it arrives.",
   "BridgeIn riceve tutte le righe prima di proseguire.": "BridgeIn receives all rows before continuing.",
   "BridgeOut con canale \"{channelName}\" non trovato in nessuna altra lane.": "BridgeOut with channel \"{channelName}\" not found in any other lane.",
@@ -532,6 +539,8 @@
   "Dal flusso": "From the flow",
   "Dark — moderno": "Dark — modern",
   "Data (da campo timestamp)": "Date (from timestamp field)",
+  "Data Quality \"${node.data.label}\", regola \"${r.label || r.id}\":": "Data Quality \"${node.data.label}\", rule \"${r.label || r.id}\":",
+  "Data Quality \"${node.data.label}\": configurazione illeggibile": "Data Quality \"${node.data.label}\": unreadable configuration",
   "Data corrente": "Current date",
   "Data corrente (2024-01-15)": "Current date (2024-01-15)",
   "Data creazione": "Creation date",
@@ -788,6 +797,7 @@
   "Formatta: format_number(x,2,\",\",\".\") → 1.234,56": "Format: format_number(x,2,\",\",\".\") → 1.234,56",
   "Formattare una data": "Format a date",
   "Frequenza & Limiti": "Frequency & limits",
+  "Functions utente — ${fnErrors.length} errore/i:\\n${d}": "User functions — ${fnErrors.length} error(s):\\n${d}",
   "Funzione DB": "DB function",
   "Funzione aggregazione cella": "Cell aggregation function",
   "Funzione utente": "User function",
@@ -1400,6 +1410,7 @@
   "Piattaforma di destinazione": "Target platform",
   "Pipeline interrotta dall'utente.": "Pipeline interrupted by the user.",
   "Piu righe": "Multiple rows",
+  "Pivot \"${node.data.label}\": campo pivot e campo valore sono obbligatori.": "Pivot \"${node.data.label}\": pivot field and value field are required.",
   "Più condizioni in cascata": "Multiple cascading conditions",
   "Policy di default della lane": "Lane default policy",
   "Policy ereditata dai nuovi nodi di questa lane": "Policy inherited by new nodes in this lane",
@@ -1859,6 +1870,8 @@
   "Torna a «passa invariato»": "Back to «pass unchanged»",
   "Totale riga": "Row total",
   "Transazione": "Transaction",
+  "Transform \"${node.data.label}\" — ${errors.length} errore/i:\\n${dettagli}": "Transform \"${node.data.label}\" — ${errors.length} error(s):\\n${dettagli}",
+  "Transform \"${node.data.label}\": configurazione campi illeggibile": "Transform \"${node.data.label}\": unreadable fields configuration",
   "Transizione da emettere": "Transition to emit",
   "Trascina campi qui · usa +campo/+{}/+[] · oppure importa da JSON": "Drag fields here · use +field/+{}/+[] · or import from JSON",
   "Trascina campi · usa +elm/@att/CDA/+grp · o importa XML/XSD": "Drag fields · use +elm/@att/CDA/+grp · or import XML/XSD",
@@ -1926,6 +1939,7 @@
   "Una volta": "Once",
   "Una volta (array JSON)": "Once (JSON array)",
   "Una volta (one-shot)": "Once (one-shot)",
+  "Union \"${node.data.label}\": mappatura campi illeggibile": "Union \"${node.data.label}\": unreadable field mapping",
   "Unisce due flussi su un campo chiave.": "Joins two flows on a key field.",
   "Unisce due oggetti (b prevale)": "Merges two objects (b wins)",
   "Unisce le righe per posizione — riga 1 di A con riga 1 di B. Richiede stesso numero di righe.": "Joins rows by position — row 1 of A with row 1 of B. Requires the same number of rows.",
@@ -2013,12 +2027,14 @@
   "Virgola separati — es: region, category, year": "Comma-separated — e.g. region, category, year",
   "Vuoto = auto-generato": "Empty = auto-generated",
   "WARN — attenzione": "WARN — warning",
+  "Window \"${node.data.label}\": configurazione funzioni illeggibile": "Window \"${node.data.label}\": unreadable functions configuration",
   "Wrap — emette { value: elemento }": "Wrap — emits { value: element }",
   "XML non valido": "Invalid XML",
   "XML — stringa grezza": "XML — raw string",
   "XSD non valido o struttura non riconosciuta": "Invalid XSD or unrecognized structure",
   "[ ] Ripetuto": "[ ] Repeating",
   "[FileReporter] Errore scrittura log:": "[FileReporter] Log write error:",
+  "[pivot \"${node.data.label}\"] valore celle vuote \"${rawNull}\" è testuale:": "[pivot \"${node.data.label}\"] empty cell value \"${rawNull}\" is text:",
   "a leggere è il motore": "the engine does the reading",
   "abilitate": "enabled",
   "aggiungi campo": "add field",
@@ -2110,6 +2126,9 @@
   "i valori del file non stanno in": "the file values don't fit in",
   "iif(cond, se_vero, se_falso)": "iif(cond, if_true, if_false)",
   "iif(condizione, a, b)": "iif(condition, a, b)",
+  "il check 'custom'": "the 'custom' check",
+  "il lookup da file non è supportato. Carica il file con": "file lookup is not supported. Load the file with",
+  "il repair 'expression'": "the 'expression' repair",
   "importa .ffpel": "import .ffpel",
   "in corso": "in progress",
   "inizio lane": "lane start",
@@ -2204,6 +2223,7 @@
   "schema unificato": "unified schema",
   "scrive nel pannello di log": "writes to the log panel",
   "scrive una variabile di lane: sopravvive fra le righe (totali, deduplica)": "writes a lane variable: survives across rows (totals, dedup)",
+  "se le colonne pivot sono numeriche, le operazioni a valle potrebbero fallire.": "if the pivot columns are numeric, downstream operations may fail.",
   "segreti richiesti:": "required secrets:",
   "seleziona testo e scegli snippet per avvolgere": "select text and choose a snippet to wrap",
   "senza nome": "unnamed",
@@ -2212,6 +2232,7 @@
   "solo il primo": "first only",
   "solo valori vuoti nel campione → dedotto": "only empty values in the sample → inferred",
   "somma delle righe": "sum of rows",
+  "source_file → materialize(\"tabella\") e usa \"lookup_from_materialize\".": "source_file → materialize(\"my_table\") and use \"lookup_from_materialize\".",
   "stringa": "string",
   "struttura": "structure",
   "substring(s, inizio [, lunghezza])": "substring(s, start [, length])",
