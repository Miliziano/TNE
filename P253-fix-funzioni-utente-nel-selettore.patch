diff --git a/src/components/FunctionPicker.tsx b/src/components/FunctionPicker.tsx
index 074e642..1e56c62 100644
--- a/src/components/FunctionPicker.tsx
+++ b/src/components/FunctionPicker.tsx
@@ -25,6 +25,7 @@ import { useMemo, useState } from 'react'
 import { FUNCTIONS } from '../ir/functions'
 import { getPresetsForType, type FieldType } from '../transforms/presets'
 import { resolveTemplate } from '../transforms/templateCompiler'
+import { activeUserFunctions } from '../ir/userFunctions'
 
 export interface VoceApplicabile {
   id:        string
@@ -87,6 +88,21 @@ export function vociApplicabili(tipo?: string): VoceApplicabile[] {
       codice: `${f.name}($sel)`,
     })
   }
+
+  // 4) le FUNZIONI UTENTE del progetto (dal registro attivo, tenuto fresco
+  //    dallo store). Nel picker `soloTrasformazioni` sono escluse dal filtro,
+  //    perché l'id `u:` non è `t:`. Il codice mette $sel come primo argomento e
+  //    i parametri restanti come segnaposto (arità corretta, da completare).
+  for (const u of activeUserFunctions()) {
+    const args = u.params.map((p, i) => (i === 0 ? '$sel' : p))
+    voci.push({
+      id: `u:${u.name}`, nome: u.name, ritorna: '—',
+      uso: `${u.name}(${u.params.join(', ')})`, desc: 'Funzione utente',
+      categoria: 'funzioni utente',
+      codice: `${u.name}(${args.join(', ')})`,
+    })
+  }
+
   return voci
 }
 
