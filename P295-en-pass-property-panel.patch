diff --git a/src/components/PropertyPanel.tsx b/src/components/PropertyPanel.tsx
index baa618b..a48e4e8 100644
--- a/src/components/PropertyPanel.tsx
+++ b/src/components/PropertyPanel.tsx
@@ -46,7 +46,7 @@ function Field({ label, children }: { label: string; children: React.ReactNode }
 
 function TabBar({ active, onChange }: { active: Tab; onChange: (t: Tab) => void }) {
   const tabs: { id: Tab; label: string; icon: string }[] = [
-    { id: 'props',     label: 'Nodo',  icon: 'ti-settings' },
+    { id: 'props',     label: 'Node',  icon: 'ti-settings' },
     { id: 'lane-vars', label: 'Lane',  icon: 'ti-variable' },
     { id: 'transactions', label: 'Transazioni', icon: 'ti-arrows-exchange' },
     { id: 'bridge',    label: 'Bridge', icon: 'ti-arrows-transfer-up' },
@@ -91,18 +91,18 @@ function MatVarChip({ variable, onDelete, onNavigate }: {
           {variable.name}
         </div>
         <div style={{ fontSize: 9, color: '#8593b5' }}>
-          materialize · in-memory per esecuzione
+          materialize · in-memory per run
         </div>
       </div>
       <button
         onClick={() => onNavigate(variable.value)}
-        title="Vai al nodo"
+        title="Go to node"
         style={{ background: 'none', border: `1px solid ${ACCENT}40`, borderRadius: 4, padding: '2px 6px', cursor: 'pointer', color: ACCENT, fontSize: 10, flexShrink: 0 }}>
         <i className="ti ti-arrow-right" style={{ fontSize: 10 }} />
       </button>
       <button
         onClick={() => onDelete(variable.id)}
-        title="Rimuovi dalla lane"
+        title="Remove from lane"
         style={{ background: 'none', border: '1px solid #3d1010', borderRadius: 4, padding: '2px 6px', cursor: 'pointer', color: '#ff5f57', fontSize: 10, flexShrink: 0 }}>
         <i className="ti ti-x" style={{ fontSize: 10 }} />
       </button>
@@ -170,7 +170,7 @@ function VariableEditor({
             background: '#3ddc84', flexShrink: 0,
             animation: 'liveVarPulse 1.5s ease-in-out infinite',
           }} />
-          Valori live — aggiornati in tempo reale dal runner
+          Live values — updated in real time by the runner
         </div>
       )}
 
@@ -213,12 +213,12 @@ function VariableEditor({
         <div style={{ margin: '4px 8px 8px' }}>
           <div style={{ fontSize: 10, color: '#5a6a8a', padding: '4px 2px', display: 'flex', alignItems: 'center', gap: 5 }}>
             <i className="ti ti-arrow-down-left" aria-hidden="true" />
-            dal pool — leggibili qui, si modificano in «Ambienti»
+            from the pool — readable here, edited in "Environments"
           </div>
           {ereditate.map((e) => (
             <div key={e.name} title={e.ombreggiata
-              ? `Una variabile locale con lo stesso nome ha la precedenza in questa lane`
-              : `Variabile condivisa: valore del profilo attivo`}
+              ? `A local variable with the same name takes precedence in this lane`
+              : `Shared variable: value of the active profile`}
               style={{
                 margin: '3px 0', padding: '5px 8px',
                 background: '#151c2c', borderRadius: 5,
@@ -240,7 +240,7 @@ function VariableEditor({
               )}
               {e.ombreggiata && (
                 <span style={{ marginLeft: 'auto', fontSize: 9, color: '#c8a060', whiteSpace: 'nowrap' }}>
-                  coperta dalla lane
+                  covered by the lane
                 </span>
               )}
             </div>
@@ -289,7 +289,7 @@ function VariableEditor({
                 pool, dove può avere un valore diverso per ogni profilo. */}
             {!running && onPromote && (
               <button onClick={() => onPromote(v.id)}
-                title={`Promuovi "${v.name}" a variabile condivisa (pool): potrà avere un valore per profilo. Esce da questa lane.`}
+                title={`Promote "${v.name}" to a shared variable (pool): it can have a value per profile. It leaves this lane.`}
                 style={{ background: 'none', border: '1px solid #2a3349', borderRadius: 4, padding: '0 6px', cursor: 'pointer', color: '#8aa4d0', fontSize: 12 }}>
                 <i className="ti ti-arrow-up" style={{ fontSize: 11 }} aria-hidden="true" />
               </button>
@@ -342,7 +342,7 @@ function VariableEditor({
               </div>
             ) : (
               <input value={v.value} onChange={(e) => onUpdate(v.id, 'value', e.target.value)}
-                placeholder="valore" style={{ ...inputStyle, flex: 1 }} />
+                placeholder="value" style={{ ...inputStyle, flex: 1 }} />
             )}
           </div>
         </div>
@@ -419,7 +419,7 @@ export function PropertyPanel() {
   if (selectedResource && resourceLaneId) {
     return (
       <aside style={getPanelStyle(true)}>
-        <div style={headerStyle}>Proprietà</div>
+        <div style={headerStyle}>Properties</div>
         <ResourcePanel resource={selectedResource} laneId={resourceLaneId} />
       </aside>
     )
@@ -427,7 +427,7 @@ export function PropertyPanel() {
 
   return (
     <aside style={getPanelStyle(expanded)}>
-      <div style={headerStyle}>Proprietà</div>
+      <div style={headerStyle}>Properties</div>
       <TabBar active={activeTab} onChange={setActiveTab} />
 
       {/* ── TAB: Nodo ── */}
@@ -436,7 +436,7 @@ export function PropertyPanel() {
           {!node || !def ? (
             <div style={emptyMsgStyle}>
               <i className="ti ti-cursor-text" style={{ fontSize: 28, display: 'block', marginBottom: 10, color: '#2a3349' }} aria-hidden="true" />
-              Seleziona un nodo sul canvas per modificarne le proprietà.
+              Select a node on the canvas to edit its properties.
             </div>
           ) : (
             <>
@@ -469,7 +469,7 @@ export function PropertyPanel() {
                 {isSpecial && (
                   <div style={{ marginTop: 6, padding: '4px 8px', background: '#3d2a0a', color: '#ffb347', border: '0.5px solid #854f0b', borderRadius: 4, fontSize: 10, display: 'flex', alignItems: 'center', gap: 4 }}>
                     <i className="ti ti-lock" style={{ fontSize: 11 }} aria-hidden="true" />
-                    Nodo obbligatorio — non eliminabile
+                    Required node — cannot be deleted
                   </div>
                 )}
               </div>
@@ -505,9 +505,9 @@ export function PropertyPanel() {
                         }}>
                           <i className="ti ti-alert-triangle" style={{ fontSize: 13, marginTop: 1 }} aria-hidden="true" />
                           <span>
-                            <b>«{dominante}» ha la precedenza.</b> Il motore esegue quella e
-                            ignora: {ignorati.map((f) => f.label).join(', ')}. Svuota il campo
-                            «{dominante}» per tornare a usarli.
+                            <b>"{dominante}" takes precedence.</b> The engine runs that one and
+                            ignores: {ignorati.map((f) => f.label).join(', ')}. Clear the
+                            "{dominante}" field to use them again.
                           </span>
                         </div>
                       )}
@@ -533,7 +533,7 @@ export function PropertyPanel() {
                                 value={node.data.props[field.key] ?? field.default}
                                 onChange={(e) => updateNodeProp(node.id, field.key, e.target.value)}
                                 disabled={inerte(field)}
-                                title={inerte(field) ? `Ignorato: ha la precedenza «${dominante}»` : undefined}
+                                title={inerte(field) ? `Ignored: "${dominante}" takes precedence` : undefined}
                                 style={inputStyle} />
                             )}
                           </Field>
@@ -542,7 +542,7 @@ export function PropertyPanel() {
                       {!isSpecial && (
                         <div style={{ margin: '8px', padding: '6px 10px', background: '#1a2030', borderRadius: 4, fontSize: 10, color: '#8593b5', border: '0.5px solid #2a3349', display: 'flex', alignItems: 'center', gap: 5 }}>
                           <i className="ti ti-mouse" style={{ fontSize: 11 }} aria-hidden="true" />
-                          Doppio click sul nodo per l'editor completo
+                          Double-click the node for the full editor
                         </div>
                       )}
                     </>
@@ -557,7 +557,7 @@ export function PropertyPanel() {
                     onMouseEnter={(e) => { (e.currentTarget as HTMLElement).style.background = 'rgba(255,95,87,0.16)' }}
                     onMouseLeave={(e) => { (e.currentTarget as HTMLElement).style.background = 'rgba(255,95,87,0.08)' }}>
                     <i className="ti ti-trash" style={{ fontSize: 12 }} aria-hidden="true" />
-                    Elimina nodo
+                    Delete node
                   </button>
                 </div>
               )}
@@ -572,7 +572,7 @@ export function PropertyPanel() {
           {!currentLane ? (
             <div style={emptyMsgStyle}>
               <i className="ti ti-hand-click" style={{ fontSize: 28, display: 'block', marginBottom: 10, color: '#2a3349' }} aria-hidden="true" />
-              Clicca sul canvas di una lane per selezionarla.
+              Click on a lane's canvas to select it.
             </div>
           ) : (
             <>
@@ -597,7 +597,7 @@ export function PropertyPanel() {
                     // scoping lessicale: a parità di nome vince la lane
                     ombreggiata: currentLane.variables.some((lv) => lv.name === v.name),
                   }))}
-                emptyMessage="Nessuna variabile locale in questa lane."
+                emptyMessage="No local variable in this lane."
                 onAdd={() => addVariable('lane', currentLane.id, { name: 'nuova_var', type: 'string', value: '' })}
                 onDelete={(id) => deleteVariable('lane', currentLane.id, id)}
                 onUpdate={(id, key, value) => updateVariable('lane', currentLane.id, id, { [key]: value } as Partial<Variable>)}
@@ -605,8 +605,8 @@ export function PropertyPanel() {
                 onNavigateMat={navigateToMatNode}
                 onPromote={(id) => {
                   const errore = promuoviVariabile(currentLane.id, id)
-                  if (errore) addLog('warn', `Promozione non riuscita — ${errore}`)
-                  else addLog('info', 'Variabile promossa a condivisa: ora ha un valore per profilo (editor «Ambienti»).')
+                  if (errore) addLog('warn', `Promotion failed — ${errore}`)
+                  else addLog('info', 'Variable promoted to shared: it now has a value per profile ("Environments" editor).')
                 }}
               />
             </>
diff --git a/src/i18n/it-en.json b/src/i18n/it-en.json
index 04eee04..c5e3d23 100644
--- a/src/i18n/it-en.json
+++ b/src/i18n/it-en.json
@@ -353,6 +353,7 @@
   "Clicca per inserire": "Click to insert",
   "Clicca per inserire nel valore:": "Click to insert into the value:",
   "Clicca per rinominare (separa il campo dai fusi)": "Click to rename (separates the field from the merged ones)",
+  "Clicca sul canvas di una lane per selezionarla.": "Click on a lane's canvas to select it.",
   "Click per vedere il messaggio completo": "Click to see the full message",
   "Client — ack manuale": "Client — manual ack",
   "Codice HTTP risposta": "HTTP response code",
@@ -577,6 +578,7 @@
   "Dopo l'elaborazione": "After processing",
   "Dopo l'input (a ramo esaurito)": "After input (when the branch is exhausted)",
   "Doppio click per la configurazione completa": "Double-click for the full configuration",
+  "Doppio click sul nodo per l'editor completo": "Double-click the node for the full editor",
   "Dove tenere traccia dei file già emessi": "Where to track already-emitted files",
   "Duplica ogni riga un numero di volte": "Duplicates each row a number of times",
   "Durable subscription (solo topic)": "Durable subscription (topic only)",
@@ -600,6 +602,7 @@
   "Elevamento a potenza": "Exponentiation",
   "Elimina": "Delete",
   "Elimina il file dal server — irreversibile": "Deletes the file from the server — irreversible",
+  "Elimina nodo": "Delete node",
   "Elimina questa versione": "Delete this version",
   "Elimina righe": "Deletes rows",
   "Elimina — solo i campi configurati sopra passano": "Drop — only the fields configured above pass",
@@ -824,6 +827,7 @@
   "Ignora namespace": "Ignore namespaces",
   "Ignora — emetti le righe così come sono": "Ignore — emit the rows as they are",
   "Ignora — né log né error_out": "Ignore — neither log nor error_out",
+  "Ignorato: ha la precedenza «${dominante}»": "Ignored: \"${dominante}\" takes precedence",
   "Il Materialize è già una hashtable — l'algoritmo è automaticamente Hash Join con accesso O(1).": "The Materialize is already a hashtable — the algorithm is automatically Hash Join with O(1) access.",
   "Il body sarà disponibile in body (stringa) e body_parsed (oggetto).": "The body will be available in body (string) and body_parsed (object).",
   "Il campo <strong>Timeout</strong> è sincronizzato con <strong>Timeout watch</strong> nel tab Configurazione — modificando uno si aggiorna l'altro.": "The <strong>Timeout</strong> field is synced with <strong>Watch timeout</strong> in the Configuration tab — changing one updates the other.",
@@ -1136,6 +1140,7 @@
   "Nessuna transazione nella lane. Creane una dal tab \"Transazioni\" del pannello proprietà.": "No transaction in this lane. Create one from the \"Transactions\" tab of the properties panel.",
   "Nessuna variabile condivisa. Aggiungine una: sarà referenziabile con ": "No shared variables. Add one: it will be referenceable with ",
   "Nessuna variabile definita nella lane. Aggiungine una dalla sidebar delle variabili.": "No variable defined in the lane. Add one from the variables sidebar.",
+  "Nessuna variabile locale in questa lane.": "No local variable in this lane.",
   "Nessuna variabile stringa disponibile in questa lane.": "No string variable available in this lane.",
   "Nessuna variabile stringa disponibile in questa lane. Aggiungila dal tab Lane nel pannello proprietà.": "No string variable available in this lane. Add it from the Lane tab in the properties panel.",
   "Nessuna variabile stringa nella lane": "No string variable in the lane",
@@ -1165,6 +1170,7 @@
   "Nodi della lane — policy errori": "Lane nodes — error policy",
   "Nodo": "Node",
   "Nodo di partenza: nessun ingresso, le righe le produce lui": "Starting node: no input, it produces the rows itself",
+  "Nodo obbligatorio — non eliminabile": "Required node — cannot be deleted",
   "Nodo trasparente — logga le righe in transito per debug. Non modifica i dati.": "Transparent node — logs the rows passing through for debugging. Does not modify the data.",
   "Nome": "Name",
   "Nome Materialize": "Materialize name",
@@ -1376,7 +1382,10 @@
   "Profilo da congelare (un ambiente per artifact)": "Profile to freeze (one environment per artifact)",
   "Progetto aperto: ${path}": "Project opened: ${path}",
   "Progetto salvato: ${path}": "Project saved: ${path}",
+  "Promozione non riuscita — ${errore}": "Promotion failed — ${errore}",
+  "Promuovi \"${v.name}\" a variabile condivisa (pool): potrà avere un valore per profilo. Esce da questa lane.": "Promote \"${v.name}\" to a shared variable (pool): it can have a value per profile. It leaves this lane.",
   "Propaga campi padre": "Propagate parent fields",
+  "Proprietà": "Properties",
   "Protocollo": "Protocol",
   "Protocollo di connessione": "Connection protocol",
   "Provenienza delle colonne": "Column provenance",
@@ -1460,6 +1469,7 @@
   "Rimuove spazi iniziali": "Removes leading spaces",
   "Rimuove spazi iniziali e finali": "Removes leading and trailing spaces",
   "Rimuovi dal keychain": "Remove from keychain",
+  "Rimuovi dalla lane": "Remove from lane",
   "Rimuovi header": "Remove header",
   "Rinomina il profilo (i valori restano)": "Rename the profile (values are kept)",
   "Rinomine applicate": "Renames applied",
@@ -1562,6 +1572,7 @@
   "Seleziona risorsa": "Select resource",
   "Seleziona tutti": "Select all",
   "Seleziona un Materialize nel tab Configurazione.": "Select a Materialize in the Configuration tab.",
+  "Seleziona un nodo sul canvas per modificarne le proprietà.": "Select a node on the canvas to edit its properties.",
   "Seleziona un template…": "Select a template…",
   "Semi-strutturato": "Semi-structured",
   "Sempre": "Always",
@@ -1673,6 +1684,7 @@
   "Svuota canvas": "Clear canvas",
   "Svuota e reinserisce": "Empties and reinserts",
   "Svuota editor": "Clear editor",
+  "Svuota il campo «{dominante}» per tornare a usarli.": "Clear the \"{dominante}\" field to use them again.",
   "Svuota query": "Clear query",
   "Svuotare l'albero?": "Empty the tree?",
   "Sì": "Yes",
@@ -1839,6 +1851,7 @@
   "Una riga per ogni elemento della struttura sorgente.": "One row for each element of the source structure.",
   "Una riga per pagina": "One row per page",
   "Una sola esecuzione — DDL, refresh viste": "A single execution — DDL, view refresh",
+  "Una variabile locale con lo stesso nome ha la precedenza in questa lane": "A local variable with the same name takes precedence in this lane",
   "Una volta": "Once",
   "Una volta (array JSON)": "Once (JSON array)",
   "Una volta (one-shot)": "Once (one-shot)",
@@ -1869,6 +1882,7 @@
   "Utente SSH": "SSH user",
   "Utile per loggare, scrivere su file o fare altro dopo il bridge.": "Useful to log, write to file or do something else after the bridge.",
   "Utile per notifiche o log di completamento senza riprocessare i dati.": "Useful for notifications or completion logs without reprocessing the data.",
+  "Vai al nodo": "Go to node",
   "Valida ogni riga contro regole configurabili — output valid e reject.": "Validates each row against configurable rules — valid and reject outputs.",
   "Valida output contro XSD": "Validate output against XSD",
   "Validazione output": "Output validation",
@@ -1887,16 +1901,19 @@
   "Valori condivisi nella lane, letti con var()": "Values shared in the lane, read with var()",
   "Valori di oggetto — { k1: {...}, k2: {...} }": "Object values — { k1: {...}, k2: {...} }",
   "Valori intermedi con let": "Intermediate values with let",
+  "Valori live — aggiornati in tempo reale dal runner": "Live values — updated in real time by the runner",
   "Valori multipli": "Multiple values",
   "Valori null": "Null values",
   "Valori predefiniti e divisioni sicure": "Default values and safe divisions",
   "Valuta": "Currency",
   "Variabile Lane": "Lane variable",
   "Variabile che contiene il path": "Variable containing the path",
+  "Variabile condivisa: valore del profilo attivo": "Shared variable: value of the active profile",
   "Variabile di Lane": "Lane variable",
   "Variabile di Lane + reset su rollback TX": "Lane variable + reset on TX rollback",
   "Variabile di tipo stringa che contiene il path del file": "String variable containing the file path",
   "Variabile di tipo stringa che contiene il path della directory": "String variable that contains the directory path",
+  "Variabile promossa a condivisa: ora ha un valore per profilo (editor «Ambienti»).": "Variable promoted to shared: it now has a value per profile (\"Environments\" editor).",
   "Variabili condivise (pool)": "Shared variables (pool)",
   "Variabili d'ambiente aggiuntive": "Additional environment variables",
   "Variabili di lane": "Lane variables",
@@ -1966,9 +1983,11 @@
   "consegna nel fine settimana": "weekend delivery",
   "content-type sconosciuto": "unknown content-type",
   "conversione": "conversion",
+  "coperta dalla lane": "covered by the lane",
   "cosa prelevare da GitHub": "what to fetch from GitHub",
   "costrutti": "constructs",
   "da quale campo prendere lo username": "which field to take the username from",
+  "dal pool — leggibili qui, si modificano in «Ambienti»": "from the pool — readable here, edited in \"Environments\"",
   "data": "date",
   "date_format(d, formato)": "date_format(d, format)",
   "descrizione regola": "rule description",
@@ -2037,6 +2056,7 @@
   "manda a valle una copia della riga": "sends a copy of the row downstream",
   "manda la riga alla porta reject": "sends the row to the reject port",
   "mappato": "mapped",
+  "materialize · in-memory per esecuzione": "materialize · in-memory per run",
   "mqtt:// — TCP (porta 1883)": "mqtt:// — TCP (port 1883)",
   "mqtts:// — TLS (porta 8883)": "mqtts:// — TLS (port 8883)",
   "nel tab Query — la modalità scrittura qui sotto è ignorata durante l'esecuzione.": "in the Query tab — the write mode below is ignored during execution.",
@@ -2146,6 +2166,7 @@
   "vuoto = branch di default del repo": "empty = repo default branch",
   "vuoto = tutte le righe — es: main.stato == \"attivo\"": "empty = all rows — e.g. main.status == \"active\"",
   "vuoto→null": "empty→null",
+  "«{dominante}» ha la precedenza.": "\"{dominante}\" takes precedence.",
   "È negativo": "Is negative",
   "È nel futuro": "Is in the future",
   "È nel passato": "Is in the past",
